import { NotzeroDirective } from './notzero.directive';

describe('NotzeroDirective', () => {
  it('should create an instance', () => {
    const directive = new NotzeroDirective();
    expect(directive).toBeTruthy();
  });
});
